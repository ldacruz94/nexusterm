export const state = {
  panels:          new Map(),
  sessions:        new Map(),
  activeId:        null,
  activeSessionId: null,
  panelCounter:    0,
  sessionCounter:  0,
  loading:         false,
};
